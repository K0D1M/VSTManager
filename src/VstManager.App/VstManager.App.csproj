<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <Content Include="a_clean_modern_app_icon_logo_design_on_a_dark_b-transparent.ico" />
    <!-- ApplicationIcon below only embeds an icon in the exe; the tray icon, notification
         balloon, Action Center sender entry and WPF window icon all load one as a *file* at
         runtime, so it has to exist next to the exe too. Without this they silently fell back to
         the generic SystemIcons.Application (and notifications showed no icon at all).
         app-icon.ico carries the same artwork with uncompressed 16..256px frames, which WPF's
         ICO decoder accepts — unlike the fully PNG-compressed original, which it throws on. -->
    <Content Include="app-icon.ico">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
  </ItemGroup>

  <ItemGroup>
    <None Include="..\..\..\Downloads\logo_package\a_clean_modern_app_icon_logo_design_on_a_dark_b.png">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\VstManager.Core\VstManager.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- UseWindowsForms below only supplies System.Drawing.Icon for the tray icon; its implicit
         global usings collide with WPF's own System.Windows / System.Windows.Forms-adjacent
         types (Color, Application, DragEventArgs, ...), so drop them and qualify explicitly
         wherever System.Drawing is actually needed. -->
    <Using Remove="System.Drawing" />
    <Using Remove="System.Windows.Forms" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.4.2" />
    <PackageReference Include="MpCoding.WPF.LoadingAnimations" Version="1.0.2" />
  </ItemGroup>

  <ItemGroup>
    <Compile Update="Properties\Settings.Designer.cs">
      <DesignTimeSharedInput>True</DesignTimeSharedInput>
      <AutoGen>True</AutoGen>
      <DependentUpon>Settings.settings</DependentUpon>
    </Compile>
  </ItemGroup>

  <ItemGroup>
    <None Update="Properties\Settings.settings">
      <Generator>SettingsSingleFileGenerator</Generator>
      <LastGenOutput>Settings.Designer.cs</LastGenOutput>
    </None>
  </ItemGroup>

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>True</UseWPF>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <Version>1.7.0</Version>
    <ApplicationIcon>a_clean_modern_app_icon_logo_design_on_a_dark_b-transparent.ico</ApplicationIcon>
    <SignAssembly>False</SignAssembly>
    <Company>$(K0D1M)</Company>
    <Product>$(KD Manager for VST)</Product>
    <PackageIcon>a_clean_modern_app_icon_logo_design_on_a_dark_b.png</PackageIcon>
    <PlatformTarget>x64</PlatformTarget>
    <Authors>$(K0D1M)</Authors>
    <PackageId>$(KDManagerForVST)</PackageId>
    <UseWindowsForms>True</UseWindowsForms>
    <Platforms>AnyCPU;ARM64</Platforms>
  </PropertyGroup>

</Project>
